spring:
  datasource:
    url: jdbc:mysql://localhost:3306/organization_db
    username: root
    password: India@123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8082

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
welcome:
  message: Hello from Organization Service Spring config refresh Demo.     

